# 数据源配置
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.70.129:3306/gulimall_wms
    driver-class-name: com.mysql.cj.jdbc.Driver

# mapper路径配置
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  # 设置主键自增
  global-config:
    db-config:
      id-type: auto
server:
  port: 8085
